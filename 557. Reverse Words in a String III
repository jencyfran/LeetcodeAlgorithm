Source Code: python 3

class Solution:
    def reverseWords(self, s: str) -> str:
        words = s.split()
        res = []
        for i in words:
            res.append(i[::-1])
            res.append(" ")
        return "".join(res).strip()


Hint:

1. Strip - removes spaces at the end and begining of the string 

2. Split example:

x = "welcome to the jungle"
x.split()
x =  ['welcome', 'to', 'the', 'jungle']
